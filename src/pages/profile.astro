---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const allPosts = await getCollection('posts');
const publishedPosts = allPosts.filter(post => !post.data.draft);

// 技能数据 - 增加图标（这里使用 emoji 或简单的文本缩写作为占位，实际项目中可替换为 SVG）
const skills = {
  frontend: [
    { name: 'Vue.js', icon: '🟢' },
    { name: 'React', icon: '🔵' },
    { name: 'TypeScript', icon: '📘' }
  ],
  backend: [
    { name: 'Node.js', icon: '💚' },
    { name: 'Rust', icon: '🦀' },
    { name: 'Python', icon: '🐍' },
    { name: 'Java', icon: '☕' },
    { name: 'MySQL', icon: '🐬' },
    { name: 'Redis', icon: '🔴' }
  ],
  tools: [
    { name: 'Git', icon: '📦' },
    { name: 'Docker', icon: '🐳' },
    { name: 'Linux', icon: '🐧' },
    { name: 'VS Code', icon: '📝' }
  ]
};

const stats = [
  { label: 'Articles', value: publishedPosts.length },
  { label: 'Tags', value: [...new Set(publishedPosts.flatMap(p => p.data.tags))].length },
  { label: 'Years', value: '2+' },
];
---

<Layout title="关于我" description="Protagonist 的个人简介">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    
    <!-- 1. Header Area: 个人信息摘要 -->
    <header class="flex flex-col md:flex-row items-start md:items-center gap-8 mb-16">
      <div class="relative group">
        <div class="w-24 h-24 bg-zinc-100 dark:bg-zinc-800 rounded-full flex items-center justify-center text-4xl border-4 border-white dark:border-zinc-950 shadow-sm overflow-hidden">
           <img src="/avatar.png" alt="Avatar" class="w-full h-full object-cover" /> 
        </div>
        <div class="absolute bottom-0 right-0 w-6 h-6 bg-green-500 border-4 border-white dark:border-zinc-950 rounded-full" title="Online"></div>
      </div>
      
      <div class="flex-1">
        <h1 class="text-3xl font-extrabold text-zinc-900 dark:text-zinc-50 tracking-tight mb-2">
          Protagonist
        </h1>
        <p class="text-zinc-500 dark:text-zinc-400 text-lg mb-4">
          Full Stack Developer & Tech Enthusiast
        </p>
        <div class="flex flex-wrap gap-3">
          <a href="https://github.com/Protagonistss" target="_blank" class="inline-flex items-center gap-2 px-3 py-1.5 bg-zinc-900 text-white rounded-lg text-sm font-medium hover:bg-zinc-700 transition-colors">
            <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>
            GitHub
          </a>
          <a href="https://protagonisths.com" target="_blank" class="inline-flex items-center gap-2 px-3 py-1.5 bg-blue-600 text-white rounded-lg text-sm font-medium hover:bg-blue-700 transition-colors">
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9a9 9 0 01-9-9m9 9c1.657 0 3-4.03 3-9s-1.343-9-3-9m0 18c-1.657 0-3-4.03-3-9s1.343-9 3-9m-9 9a9 9 0 019-9"/></svg>
            Website
          </a>
        </div>
      </div>

      <!-- Stats Cards (Mini) -->
      <div class="hidden md:flex gap-4">
        {stats.map(stat => (
          <div class="px-5 py-3 bg-zinc-50 dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-800 rounded-xl text-center">
            <div class="text-xl font-bold text-zinc-900 dark:text-zinc-100">{stat.value}</div>
            <div class="text-xs text-zinc-500 uppercase tracking-wide font-medium mt-0.5">{stat.label}</div>
          </div>
        ))}
      </div>
    </header>

    <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
      <!-- 2. Main Content: About -->
      <main class="lg:col-span-2 space-y-8">
        <section>
          <h2 class="text-xl font-bold text-zinc-900 dark:text-zinc-100 mb-4 flex items-center gap-2">
            <span class="text-xl">👋</span> 关于我
          </h2>
          <div class="prose prose-zinc dark:prose-invert max-w-none text-zinc-600 dark:text-zinc-400">
            <p>
              嗨，我是 Protagonist。一名热爱技术、追求极致体验的全栈开发者。
            </p>
            <p>
              我热衷于探索 Web 技术的无限可能，从构建高性能的前端界面到设计健壮的后端架构。在 coding 之外，我也喜欢写博客记录学习心得，分享技术见解。
            </p>
            <p>
              我相信代码不仅是逻辑的堆砌，更是艺术的表达。致力于编写简洁、优雅且易于维护的代码是我的职业追求。
            </p>
          </div>
        </section>

        <!-- Current Focus / What I'm doing -->
        <section class="bg-blue-50/50 dark:bg-blue-900/10 rounded-2xl p-6 border border-blue-100 dark:border-blue-800/30">
          <h3 class="font-bold text-zinc-900 dark:text-zinc-100 mb-3 flex items-center gap-2">
            <span class="w-2 h-2 bg-blue-500 rounded-full animate-pulse"></span>
            当前聚焦
          </h3>
          <ul class="space-y-2 text-sm text-zinc-600 dark:text-zinc-400">
            <li class="flex items-start gap-2">
              <span class="mt-1.5 w-1 h-1 bg-zinc-400 rounded-full"></span>
              深入研究 Rust 框架与其生态系统
            </li>
            <li class="flex items-start gap-2">
              <span class="mt-1.5 w-1 h-1 bg-zinc-400 rounded-full"></span>
              探索 AI 辅助编程的最佳实践
            </li>
            <li class="flex items-start gap-2">
              <span class="mt-1.5 w-1 h-1 bg-zinc-400 rounded-full"></span>
              重构个人知识库系统
            </li>
          </ul>
        </section>
      </main>

      <!-- 3. Sidebar: Tech Stack -->
      <aside class="space-y-6">
        <div class="bg-white dark:bg-zinc-900 rounded-2xl border border-zinc-200 dark:border-zinc-800 p-6 shadow-sm">
          <h2 class="font-bold text-zinc-900 dark:text-zinc-100 mb-6 flex items-center gap-2">
            <svg class="w-5 h-5 text-zinc-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"/></svg>
            技术栈
          </h2>
          
          <div class="space-y-6">
            <div>
              <div class="text-xs font-semibold text-zinc-400 uppercase tracking-wider mb-3">Frontend</div>
              <div class="flex flex-wrap gap-2">
                {skills.frontend.map(skill => (
                  <span class="inline-flex items-center gap-1.5 px-2.5 py-1 bg-zinc-50 dark:bg-zinc-800 text-zinc-700 dark:text-zinc-300 rounded-md text-sm border border-zinc-100 dark:border-zinc-700/50 hover:border-blue-200 dark:hover:border-blue-800 transition-colors cursor-default">
                    <span class="text-xs opacity-70">{skill.icon}</span> {skill.name}
                  </span>
                ))}
              </div>
            </div>
            
            <div>
              <div class="text-xs font-semibold text-zinc-400 uppercase tracking-wider mb-3">Backend</div>
              <div class="flex flex-wrap gap-2">
                {skills.backend.map(skill => (
                  <span class="inline-flex items-center gap-1.5 px-2.5 py-1 bg-zinc-50 dark:bg-zinc-800 text-zinc-700 dark:text-zinc-300 rounded-md text-sm border border-zinc-100 dark:border-zinc-700/50 hover:border-emerald-200 dark:hover:border-emerald-800 transition-colors cursor-default">
                    <span class="text-xs opacity-70">{skill.icon}</span> {skill.name}
                  </span>
                ))}
              </div>
            </div>
            
            <div>
              <div class="text-xs font-semibold text-zinc-400 uppercase tracking-wider mb-3">Toolkit</div>
              <div class="flex flex-wrap gap-2">
                {skills.tools.map(skill => (
                  <span class="inline-flex items-center gap-1.5 px-2.5 py-1 bg-zinc-50 dark:bg-zinc-800 text-zinc-700 dark:text-zinc-300 rounded-md text-sm border border-zinc-100 dark:border-zinc-700/50 hover:border-zinc-300 dark:hover:border-zinc-600 transition-colors cursor-default">
                    <span class="text-xs opacity-70">{skill.icon}</span> {skill.name}
                  </span>
                ))}
              </div>
            </div>
          </div>
        </div>

        <!-- Mobile Stats (Visible only on mobile) -->
        <div class="grid grid-cols-3 gap-3 md:hidden">
          {stats.map(stat => (
            <div class="p-3 bg-zinc-50 dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-800 rounded-xl text-center">
              <div class="text-lg font-bold text-zinc-900 dark:text-zinc-100">{stat.value}</div>
              <div class="text-[10px] text-zinc-500 uppercase tracking-wide font-medium mt-0.5">{stat.label}</div>
            </div>
          ))}
        </div>
      </aside>
    </div>
  </div>
</Layout>