---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import { getTagStats, getCategoryStats, groupPostsByTechStack } from '../utils/posts';
import { getTechStackIcon } from '../utils/techStack';

const allPosts = await getCollection('posts');
const publishedPosts = allPosts.filter(post => !post.data.draft);

// 按日期排序
publishedPosts.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

// 数据获取
const tagStats = getTagStats(publishedPosts);
const categoryStats = getCategoryStats(publishedPosts);
const postsByTechStack = groupPostsByTechStack(publishedPosts);

// 技术栈排序：按文章数量降序
const sortedTechStacks = Object.entries(postsByTechStack).sort((a, b) => b[1].length - a[1].length);
---

<Layout title="知识库索引" description="技术栈与分类导航">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    
    <header class="mb-12 border-b border-zinc-100 dark:border-zinc-800 pb-8">
      <h1 class="text-3xl font-bold text-zinc-900 dark:text-zinc-50 tracking-tight">
        知识库索引
      </h1>
      <p class="text-zinc-500 dark:text-zinc-400 mt-2 text-lg">
        按技术栈与主题整理的 {publishedPosts.length} 篇技术沉淀
      </p>
    </header>

    <div class="grid grid-cols-1 lg:grid-cols-12 gap-12">
      
      <!-- 左侧主区域：技术栈目录 (占据 8/12) -->
      <main class="lg:col-span-8 space-y-12">
        <section>
          <h2 class="text-xl font-bold text-zinc-900 dark:text-zinc-100 mb-6 flex items-center gap-2">
            <span class="w-1.5 h-6 bg-blue-600 rounded-full"></span>
            Tech Stacks
          </h2>
          
          <div class="grid gap-6 md:grid-cols-2">
            {sortedTechStacks.map(([techStack, posts]) => (
              <div class="bg-white dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-800 rounded-xl p-5 hover:border-blue-300 dark:hover:border-blue-700 transition-colors shadow-sm">
                <!-- 卡片头部 -->
                <div class="flex items-center justify-between mb-4">
                  <a href={`/categories/${techStack}`} class="flex items-center gap-3 group">
                    <div class="w-10 h-10 bg-zinc-50 dark:bg-zinc-800 rounded-lg flex items-center justify-center text-xl group-hover:scale-110 transition-transform">
                      {getTechStackIcon(techStack)}
                    </div>
                    <div>
                      <h3 class="font-bold text-zinc-900 dark:text-zinc-100 group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors">
                        {techStack}
                      </h3>
                      <span class="text-xs text-zinc-500 font-medium">{posts.length} 篇文章</span>
                    </div>
                  </a>
                  <a href={`/categories/${techStack}`} class="p-2 text-zinc-400 hover:text-blue-600 dark:hover:text-blue-400 transition-colors">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"/>
                    </svg>
                  </a>
                </div>

                <!-- 最新文章列表预览 -->
                <ul class="space-y-3">
                  {posts.slice(0, 3).map(post => (
                    <li>
                      <a href={`/posts/${post.slug}`} class="block group/link">
                        <h4 class="text-sm font-medium text-zinc-700 dark:text-zinc-300 group-hover/link:text-blue-600 dark:group-hover/link:text-blue-400 truncate transition-colors">
                          {post.data.title}
                        </h4>
                        <div class="flex items-center gap-2 mt-1">
                          <time class="text-[10px] text-zinc-400 tabular-nums">
                            {post.data.pubDate.toLocaleDateString('zh-CN')}
                          </time>
                          {post.data.categories[0] && (
                            <span class="text-[10px] px-1.5 py-0.5 bg-zinc-100 dark:bg-zinc-800 text-zinc-500 rounded-sm">
                              {post.data.categories[0]}
                            </span>
                          )}
                        </div>
                      </a>
                    </li>
                  ))}
                </ul>
                
                {posts.length > 3 && (
                  <div class="mt-4 pt-3 border-t border-zinc-50 dark:border-zinc-800/50 text-center">
                    <a href={`/categories/${techStack}`} class="text-xs font-medium text-zinc-400 hover:text-blue-600 dark:hover:text-blue-400 transition-colors">
                      查看其余 {posts.length - 3} 篇文章
                    </a>
                  </div>
                )}
              </div>
            ))}
          </div>
        </section>
      </main>

      <!-- 右侧侧边栏：分类与标签 (占据 4/12) -->
      <aside class="lg:col-span-4 space-y-10">
        
        <!-- 分类列表 (Topics) -->
        <section>
          <h2 class="text-lg font-bold text-zinc-900 dark:text-zinc-100 mb-5 flex items-center gap-2">
            <span class="text-blue-500">📂</span> Topics
          </h2>
          <div class="flex flex-col gap-2">
            {categoryStats.map(({ category, count }) => (
              <a 
                href={`/categories/${category}`}
                class="flex items-center justify-between p-3 rounded-lg bg-zinc-50 dark:bg-zinc-900/50 hover:bg-white dark:hover:bg-zinc-800 border border-transparent hover:border-zinc-200 dark:hover:border-zinc-700 transition-all group"
              >
                <span class="text-sm font-medium text-zinc-700 dark:text-zinc-300 group-hover:text-blue-600 dark:group-hover:text-blue-400">
                  {category}
                </span>
                <span class="px-2 py-0.5 text-xs bg-zinc-200 dark:bg-zinc-800 text-zinc-500 dark:text-zinc-400 rounded-full">
                  {count}
                </span>
              </a>
            ))}
          </div>
        </section>

        <!-- 热门标签 (Tags) -->
        <section>
          <h2 class="text-lg font-bold text-zinc-900 dark:text-zinc-100 mb-5 flex items-center gap-2">
            <span class="text-blue-500">#</span> Popular Tags
          </h2>
          <div class="flex flex-wrap gap-2">
            {tagStats.slice(0, 20).map(({ tag, count }) => (
              <a 
                href={`/tags/${tag}`}
                class="inline-flex items-center gap-1.5 px-3 py-1.5 bg-white dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-800 rounded-md text-xs font-medium text-zinc-600 dark:text-zinc-400 hover:text-blue-600 dark:hover:text-blue-400 hover:border-blue-200 dark:hover:border-blue-800 transition-all"
              >
                {tag}
                <span class="opacity-50 text-[10px]">{count}</span>
              </a>
            ))}
          </div>
          <div class="mt-4 text-right">
            <a href="/tags" class="text-xs font-medium text-blue-600 hover:text-blue-700 hover:underline">
              View all tags →
            </a>
          </div>
        </section>

      </aside>
    </div>
  </div>
</Layout>